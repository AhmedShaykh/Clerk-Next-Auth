import Signals from "@/Components/Signals";

const Home = () => {
    return (
        <div className="flex justify-center items-center h-screen">
            <Signals />
        </div>
    )
};

export default Home;